# Сітка 108 клітинок: BTCUSDT, рушій `mamdani`

Згенеровано `scripts/run_grid.py`; усі числа — з цього прогону.

```
uv run python scripts/run_grid.py --db --symbol BTCUSDT --engine mamdani --workers 8
```

| поле | значення |
|---|---|
| набір | db:BTCUSDT · BTCUSDT 1m · 64800 барів · 2026-08-04T00:00Z … 2026-09-17T23:59Z |
| dataset_hash (свічки + інструмент + фандинг) | `99382675fda6dcab9be0fa566ac6c602686bf19685aa9c5557f5b7cdf3b8c345` |
| хеш лише свічок (як data/dataset_window.json) | `41bc9f0b20a8d4d2cf99234f21f4e6592196cf9c6151e8e87dba06e6fef6dcd5` |
| config_hash (база, без параметрів сітки) | `48e2a5d88b321d31c7b415370de5ed6505a2af4175883ddb245fbdcf9dfaa1c6` |
| git_sha | `b93380254e142a2766ecfb4ea875819ef6955567` |
| рушій / витрати / COOLDOWN | mamdani / full / scaled_entries |
| seed / воркери / прогрів | 20260919 / 8 / 523 барів |
| створено (UTC) | 2026-09-19T14:28:30Z |

## Робоча точка з фронту Парето

Критерії фронту: SR↑, MaxDD↓, Turnover↓ на **конкатенованому OOS** (108 клітинок; фолдів OOS: 6).

ε-обмеження на фронті Парето (SR↑, MaxDD↓, Turnover↓): серед недомінованих клітинок з MaxDD ≤ dd_cap обирається клітинка з найбільшим Шарпом; рівність → менший оборот → менший індекс сітки. Якщо жодна клітинка фронту не вкладається в dd_cap — клітинка фронту з найменшою MaxDD (рівність → більший Шарп → менший оборот → менший індекс). dd_cap за замовчуванням = state_machine.cool_enter (просадка, з якої автомат ризику переходить у COOLDOWN): робоча точка, що вже на IS доходить до COOLDOWN, неприйнятна.

dd_cap = 0.08; фронт — 2 клітинок, з них у межі dd_cap — 0; правило спрацювало як `fallback_min_dd`.

**Обрано клітинку 33:** n_atr = 7, chi = 2.5, u_enter = 0.3, rho_base = 0.0025, lam = 0.97

Точку обрано ЗА OOS-метриками всіх клітинок, тож її OOS-метрики вже не є незалежною оцінкою: поправку на вибір із N клітинок дає DSR нижче; незалежна поза-вибіркова оцінка ПРОЦЕДУРИ вибору — walk-forward (`scripts/run_walkforward.py`: вибір на IS, оцінка на OOS).

### Фронт

| клітинка | n_ATR | χ | u_enter | ρ_base | λ | SR oos | MaxDD oos | оборот oos | дох. oos | SR усе вікно | MaxDD усе вікно | обрана |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 32 | 7 | 2.5 | 0.3 | 0.0025 | 0.94 | -75.8893 | 0.2451 | 8 768.6 | -0.2451 | -46.5099 | 0.1202 |  |
| 33 | 7 | 2.5 | 0.3 | 0.0025 | 0.97 | -76.769 | 0.238 | 8 458.0 | -0.238 | -46.2647 | 0.1202 | так |

## PSR і DSR (Bailey & López de Prado; Шарп за період, SR* = 0 для PSR)

| величина | простір вибору (oos) | усе вікно (in-sample, довідково) |
|---|---|---|
| N (фактично оцінених клітинок) | 108 | 108 |
| скінченних SR | 108 | 108 |
| Var(SR_i), вибіркова | 0.0008 | 8.717e-06 |
| SR₀ = E[max SR] | 0.0717 | 0.0076 |
| ŜR обраної (за період) | -0.1059 | -0.0638 |
| n доходностей | 43 194 | 64 277 |
| γ₃ / γ₄ | -5.1813 / 69.1209 | -12.4645 / 353.7025 |
| PSR | 0 | 0 |
| DSR | 0 | 0 |

**обрана точка, конкатенований OOS: PSR = 0 < 0.95 — перевага над нульовим Шарпом статистично НЕ встановлена; DSR = 0 при N = 108 (SR₀ = 0.07175 за період).**

та сама клітинка, усе вікно (in-sample): PSR = 0 < 0.95 — перевага над нульовим Шарпом статистично НЕ встановлена; DSR = 0 при N = 108 (SR₀ = 0.007551 за період).

Розподіл річного SR (oos) по 108 клітинках зі скінченним SR: min -131.9328, медіана -106.9052, max -75.8893.

## Паспорти в БД (run kind = grid_cell)

Стани запису: written: 108; перечитано в транзакції і збігається з воркером (статус, config_hash, equity_hash): 108 з 108.

Повна таблиця клітинок (параметри, метрики всього вікна і OOS, хеші, run_id) — у CSV поруч.
