# Журнал робіт: фінальний прохід документації (фаза 10, звітні числа)

Автор: Андрій Жук, 2026.

**2026-09-19, 20:55–21:15 (+03:00), після коміту `415acbd`.** Мета — закрити gate фази 10 «у docs/ немає жодного маркера TBD»
виміряними числами, а не обіцянками. Робив у фіксованому порядку, щоб кожне число мало паспорт чистого коду. Поки не з'явились
усі вимірювані виводи, поза `docs/` нічого не редагував.

(1) **Прогін фази 6.** Перезапустив його на чистому HEAD. Run `4e5be0de-a4b7-4ef1-b84e-4a563b248be3`, `git_dirty = 0`, «chain OK,
anchor OK». `equity_hash 3b5009cd…` той самий, що в брудного прогону `a848fa56` на `b933802`, тож крива відтворилась побітово.

(2) **Тести, покриття, lint, аудит.** `make test` — 979 passed за 12.83 с, послідовно — 29.90 с. Разом з інтеграційними на
тестовій БД 5443 — 1034 passed. Об'єднане покриття — 89.38 %; fuzzy 99.4, risk 99.9, decision 98.7, sizing 100.0. Офлайн-покриття —
83.72 %. `make lint` чистий. `pip-audit` — «No known vulnerabilities found»: це закрило маркер, що висів з моменту вилучення
python-jose. Виводи — у `docs/report_tables/raw/`. Тестову БД після прогону зупинив.

(3) **Таблиці звіту.** Перший запуск `export_report_tables.py` з двома вкладеними каталогами результатів продублював таблиці
exp_search (FIN-02). Перезапустив з одним каталогом. Поіменний перелік тестів, який експорт перезаписав, згенерував заново його
власним генератором. Коротку версію експорту зберіг окремим файлом (FIN-03).

(4) **Маркери.** Кожен маркер у `docs/` замінено або числом з джерелом, або явним «не виконано: причина; що зробити і ким». Другий
варіант — для кроків людини і для артефактів фази 10–11, яких агент не створював (FIN-07). Для ТЗ §4.4 окремо поміряв пам'ять
(FIN-08): бектест 45 днів у пам'яті — 243 МБ max RSS, імпорт API — 96 МБ.

(5) **`docs/results.md`.** Написав підрозділ 2.11 звіту: калібрування, паспорт фази 6, сітки, walk-forward, чутливість, три моделі
витрат, ablation, VaR/Купець, гістерезис, Амдал, MLP, патологічні сесії, крос-звірка, монотонність, COOLDOWN і таблиця трасування.
Головний висновок чесний. Без витрат сигнал BTCUSDT має слабку валову перевагу: PSR 0.9727, без поправки на множинний вибір
(FIN-06). З повними витратами збиткове все, PSR = DSR = 0. Засувка HALTED зупиняє кожну з 216 клітинок сітки на просадці
12.00–12.04 %.

(6) **Корінь репозиторію.** Перенесено README (uk/en) і CHANGELOG з `docs/_pending_root/` з фінальними числами. Перевірено
неруйнівні команди швидкого старту; `make backup` без `backups/` і дампи поза `.gitignore` записано як FIN-09.

По дорозі знайшов і записав, не виправляючи чужого коду: `make report` дублює таблиці (FIN-02); три жорстко закодовані маркери в
трасуванні (FIN-04); прапорець `git_dirty` і шаблонний текст ETH-моделі MLP (FIN-05).
