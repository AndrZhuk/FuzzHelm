# Журнал робіт: компонент wiring (фаза 10)

Автор: Андрій Жук, 2026.

**2026-09-19.** Довів до робочого контуру те, що до цього існувало лише в тестах і звітах. (1) MLP-автокодувальник
аномалій тепер справді стоїть у QualityGate §4.1. Спершу перезапустив оцінювання на робочій БД і отримав побайтово ту
саму таблицю ROC-AUC, що в `docs/figures/quality_mlp_rocauc.md`; потім — розбиття всередині IS (навчання дні 1–12,
перевірка 13–15), щоб рішення не спиралось на відкладені дні: 5-3-5 кращий за 8-3-8 в усіх 6 seed в обох розбиттях
(0.9718 ± 0.0021 проти 0.9551 і 0.9792 ± 0.0045 проти 0.9567), тож робоча архітектура — 5-3-5, тобто рівно вектор
§5.17. Модель збережено JSON-артефактом `data/anomaly_mlp_BTCUSDT.json` (поріг q₉₉ 2.865277…, ті самі 21 600 барів, що й
калібрування МФ), а не pickle: його видно в git, а мережу відтворює кілька рядків numpy, які повторюють
`StandardScaler` і `MLPRegressor.predict` так точно, що скори збігаються до біта. Конвеєр прогріває екстрактор 218
барами перед першою свічкою (БД, за потреби REST), віддає скор до свічки, щоб воркер записав обидва однією транзакцією,
і рахує аномалії в `validity` години Q. На 45 днях BTCUSDT модель позначає 1.00 % барів IS і 1.21 % барів днів 16–45;
на чистому реплеї — жодного бару, на flash_crash — 10 з 45, причому крива капіталу в обох реплеях та сама. Скор
обрізаю до меж колонки NUMERIC(10,6) — інакше обвал ціни зламав би запис свічки. (2) Сторож тиші живого WebSocket
перейшов на монотонний годинник (WS-07): тест показує, що крок NTP +40 с раніше рвав здорове з'єднання, а крок −40 с
ховав справжню 15-секундну тишу; тепер ні те, ні інше. (3) «Брудний код» визначено один раз — зміни поза `docs/` і
`artifacts/` (`manifest.read_git_state`), і так тепер рахують паспорти POST /backtests, торгового воркера і
`run_backtest.py`; сирий `read_git_sha` лишив для сумісності зі скриптами експериментів (їхній тест перевіряє саме
сирий прапорець), а новий тест звіряє всі три реалізації в тимчасовому репозиторії. (4) CI з чотирма задачами
(lint, test з порогом покриття з pyproject, integration на PostgreSQL 16 з захистом від «зеленого» пропуску, pip-audit),
`fly.toml` для API (не деплоїв — це крок людини) і нові цілі Makefile (`experiments`, `walkforward`, `report --db`,
`anomaly`, `audit`, `users`). Локально: покриття 86.46 %, `pip-audit` — вразливостей немає. Інтеграційні тести не
запускав: тестова БД належить іншому компоненту.

**2026-09-19 (рецензія).** Незалежно перевірив проводку: перезапустив навчання (артефакт відтворився до дайджесту
параметрів, звіт ROC-AUC — побайтово), перезаміряв внутрішнє розбиття IS, частки аномалій на 45 днях і прогрів з
робочої БД (лише читання), прогнав `IngestPipeline` зі справжньою моделлю по чотирьох записаних сесіях і реплеї
торгового воркера. Знайшов і закрив два дефекти торгового контуру (WIRE-07): скор свічки потоку губився, якщо перед нею
торговий цикл добирав REST-бари дірки, а свічка, що перекривається з прогрівом, удруге проходила через рекурсивний стан
екстрактора. Тепер скорер приймає лише строго новіші бари, а вердикт дістається тільки своїй свічці; обидва випадки
закріплено тестами, що падали до виправлення. Реплеї й повний набір після змін — ті самі числа.
